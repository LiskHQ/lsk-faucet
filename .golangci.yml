run:
  timeout: 5m
  exclude-dirs:
    - .github
    - web

linters-settings:
  misspell:
    locale: US

linters:
  disable-all: true
  enable:
    - gosimple
    - govet
    - ineffassign
    - goconst
    - goimports
    - misspell
    - revive
    - unconvert
    - unused
